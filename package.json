{
  "name": "affiliate-api-auto-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright API regression tests for Kuant affiliate referral APIs",
  "scripts": {
    "start": "node server.js",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "smoke:case-runner": "node scripts/smoke-case-runner.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0",
    "dotenv": "^16.6.1"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
